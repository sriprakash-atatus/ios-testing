issuer: https://gitlab.ddbuild.io

subject_pattern: "project_path:atatus/atatus-sdk-ios:ref_type:(branch|tag):.*"

claim_pattern:
  project_path: "atatus/atatus-sdk-ios"
  ref_type: "(branch|tag)"
  ci_config_ref_uri: "gitlab.ddbuild.io/atatus/atatus-sdk-ios//.gitlab-ci.yml@.*"

permissions:
  metadata: read
