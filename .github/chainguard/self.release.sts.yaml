issuer: https://gitlab.ddbuild.io

subject_pattern: "project_path:atatus/atatus-sdk-ios:ref_type:tag:ref:.*"

claim_pattern:
  project_path: "atatus/atatus-sdk-ios"
  ref_type: "tag"
  pipeline_source: "push"
  ci_config_ref_uri: "gitlab.ddbuild.io/atatus/atatus-sdk-ios//.gitlab-ci.yml@.*"

permissions:
  contents: write
